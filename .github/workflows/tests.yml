name: tests

on:
  push:
    branches: [main]
  pull_request:

jobs:
  test:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Fetch replay parser (official release, SHA-256 pinned)
        run: node tools/fetch-rrrocket.mjs
      - name: Run invariant + golden-replay tests
        run: npm test

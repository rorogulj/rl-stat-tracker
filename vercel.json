{
  "github": { "silent": true },
  "buildCommand": "cd client && npm install && npm run build && cp -r ../demo-data dist/demo",
  "outputDirectory": "client/dist",
  "rewrites": [{ "source": "/((?!api/).*)", "destination": "/index.html" }]
}

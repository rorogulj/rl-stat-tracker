node_modules/

# downloaded ballchasing corpus (~27k replays) — the repo only carries the download tool
benchmark-replays/

# external replay datasets (Kaggle high-level GC/SSL dump etc.) — never committed
kaggle-replays/
dataset/
datasets/

# local data: SQLite database, API key (ballchasing.key!), trained GBDT models
server/data/

# build output (served from client/dist, built with `npm run build`)
client/dist/

# replay parser binary — downloaded from the official rrrocket release with a
# pinned SHA-256 by tools/fetch-rrrocket.mjs; never committed
tools/rrrocket.exe

*.log

# incidental screenshots
zen_*.png
